# PolynomialInterpolation
Simple and efficient linear interpolation class in C++.

* __Flexible Initialization__ : Supports custom range setup for both input (ADC) and output values.
* __Efficient Calculation__ : Provides fast linear interpolation (calculate) based on pre-computed offsets.
* __Lightweight__ : Zero external dependencies, making it perfect for embedded systems or small projects.
